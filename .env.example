# ==========================================
# TEIKON POS - Variables de Entorno
# ==========================================

# Base de Datos (PostgreSQL)
# Obtén esta URL desde tu proveedor de base de datos (Neon, Supabase, etc.)
DATABASE_URL=postgresql://db_ug5ykojy87dn:VCIlpoz80aKCC1kCgJLIZMXs@up-de-fra1-postgresql-1.db.run-on-seenode.com:11550/db_ug5ykojy87dn

# Seguridad - JWT Secret
# CRÍTICO: Genera una clave secreta fuerte y única para producción
# Ejemplo: openssl rand -base64 32
JWT_SECRET=2f6620b7949fed630a8e17e7bc34fc5af91e315a78066994da1f0c6b6dffe8d94929792d141df2f249d70e5412a57dea0cf2bf163d66b3ff3f485e1abbaf2131

# Puerto del Servidor (Opcional)
# Dejar vacío en producción - SeeNode asigna automáticamente
# Solo usar 5000 para desarrollo local
PORT=
# VITE_API_URL - Usar ruta relativa (vacía) para producción
VITE_API_URL=

# ==========================================
# CONFIGURACIÓN DE PRODUCCIÓN (SEENODE)
# ==========================================

# JWT Configuration
# Tiempo de expiración del token (90 días para producción, 30d para desarrollo)
JWT_EXPIRATION=90d

# Production Domain (para CORS)
# Cambiar a tu dominio real de Seenode
PRODUCTION_URL=https://tu-app.run-on-seenode.com

# Trust Proxy (para balanceador de carga de Seenode)
# Esto permite que Express confíe en los headers del proxy
# CRÍTICO: Debe estar en 'true' para producción en Seenode
TRUST_PROXIES=true

# Node Environment
# Valores: development | production
NODE_ENV=production

# ==========================================
# NOTAS IMPORTANTES
# ==========================================
# 1. NUNCA subas este archivo con valores reales a Git
# 2. Crea un archivo .env local con tus valores reales
# 3. El archivo .env debe estar en .gitignore
# 4. JWT_SECRET es OBLIGATORIO - el servidor no iniciará sin él
# 5. PRODUCTION_URL debe coincidir con tu dominio real en Seenode
# 6. TRUST_PROXIES=true es OBLIGATORIO para Seenode (evita errores de sesión)
# 7. JWT_EXPIRATION=90d recomendado para producción (evita expiración frecuente)

